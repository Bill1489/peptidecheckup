"use client";

/**
 * PLACEHOLDER — replaced by the commerce build. Keeps the site layout compiling.
 * The real drawer renders cart lines, quantities, totals, promo code, research
 * acknowledgement notice and the checkout CTA.
 */
export function CartDrawer() {
  return null;
}
