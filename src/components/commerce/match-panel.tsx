"use client";

import type { Product } from "@/data/products/types";

/**
 * PLACEHOLDER — replaced by the assessment/match build.
 * Renders the "Your match" panel on a product page when the stored match
 * result's primary product is this product.
 */
export function MatchPanel({ product }: { product: Product }) {
  void product;
  return null;
}
